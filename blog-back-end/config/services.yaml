# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
  account_upload_dir: '%kernel.project_dir%/public/uploads/accounts'
  account_upload_path: '/uploads/accounts'
  post_upload_dir: '%kernel.project_dir%/public/uploads/posts'
  post_upload_path: '/uploads/posts'

services:
  # default configuration for services in *this* file
  _defaults:
    autowire: true      # Automatically injects dependencies in your services.
    autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

  Elastic\Elasticsearch\Client:
    factory: ['Elastic\Elasticsearch\ClientBuilder', 'fromConfig']
    arguments:
      - { hosts: ['%env(ELASTIC_HOST)%'] }
  App\EventSubscriber\PostIndexSubscriber:
    tags: ['doctrine.event_subscriber']
  # makes classes in src/ available to be used as services
  # this creates a service per class whose id is the fully-qualified class name
  App\:
    resource: '../src/'
    exclude:
      - '../src/DependencyInjection/'
      - '../src/Entity/'
      - '../src/Kernel.php'
  App\Service\LocalFileUploadService:
    lazy: true
  App\Service\SendEmailService:
    lazy: true

  App\Service\AccountService:
    arguments:
      $uploadDir: '%account_upload_dir%'
      $uploadPath: '%account_upload_path%'

  App\Service\PostService:
    arguments:
      $uploadPostDir: '%post_upload_dir%'
      $uploadPostPath: '%post_upload_path%'

  App\Listener\AbstructUserListener:
    tags:
      - { name: 'doctrine.event_listener', event: 'prePersist' }
      - { name: 'doctrine.event_listener', event: 'preUpdate' }
    # add more service definitions when explicit configuration is needed
  # please note that last definitions always *replace* previous ones
